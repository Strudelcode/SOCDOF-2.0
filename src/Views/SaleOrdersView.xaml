<UserControl x:Class="SOCDOF.Views.SaleOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid Margin="0,28,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel>
                <TextBlock FontSize="14"
                           Foreground="{StaticResource MutedTextBrush}"
                           Text="Verkaufsaufträge und Auftragswerte lokal verwalten" />
                <TextBlock x:Name="OrderCountText"
                           Margin="0,8,0,0"
                           FontSize="12"
                           Foreground="{StaticResource MutedTextBrush}" />
            </StackPanel>

            <Button HorizontalAlignment="Right"
                    Click="NewOrderButton_OnClick"
                    Content="+  Neuen Verkauf anlegen"
                    Style="{StaticResource PrimaryButtonStyle}" />
        </Grid>

        <Grid x:Name="OrderToolbar" Grid.Row="1" Margin="0,24,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" MaxWidth="420" HorizontalAlignment="Left">
                <TextBox x:Name="SearchBox"
                         Padding="12,9"
                         FontSize="14"
                         TextChanged="SearchBox_OnTextChanged"
                         ToolTip="Verkäufe suchen" />
                <TextBlock Margin="14,0,0,0"
                           VerticalAlignment="Center"
                           IsHitTestVisible="False"
                           Foreground="{StaticResource MutedTextBrush}"
                           Text="Verkäufe suchen...">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <TextBlock Grid.Column="1"
                       Margin="20,0,0,0"
                       VerticalAlignment="Center"
                       Foreground="{StaticResource MutedTextBrush}"
                       Text="Abgeschlossene Verkäufe" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.35*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <DataGrid x:Name="OrderGrid"
                      Grid.Column="0"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      IsReadOnly="True"
                      SelectionChanged="OrderGrid_OnSelectionChanged"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      RowHeaderWidth="0"
                      Background="White"
                      BorderBrush="{StaticResource BorderBrush}"
                      BorderThickness="1"
                      Foreground="{StaticResource TextBrush}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Auftragsnummer" Binding="{Binding OrderNumber}" Width="1.6*" />
                    <DataGridTextColumn Header="Datum" Binding="{Binding OrderDate, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Width="1.4*" />
                    <DataGridTextColumn Header="Partner" Binding="{Binding PartnerName}" Width="2*" />
                    <DataGridTextColumn Header="Gesamtsumme" Binding="{Binding TotalAmount, StringFormat={}{0:N2}}" Width="1.4*" />
                </DataGrid.Columns>
            </DataGrid>

            <Border x:Name="OrderDetailPanel"
                    Grid.Column="2"
                    Padding="24"
                    Background="White"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    Visibility="Collapsed">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}"
                               Text="Auftragsdetails" />
                    <TextBlock x:Name="DetailOrderNumberText"
                               Grid.Row="1"
                               Margin="0,16,0,0"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource AccentDarkBrush}" />
                    <TextBlock x:Name="DetailPartnerText"
                               Grid.Row="2"
                               Margin="0,8,0,0"
                               Foreground="{StaticResource TextBrush}" />
                    <TextBlock x:Name="DetailDateText"
                               Grid.Row="3"
                               Margin="0,8,0,0"
                               Foreground="{StaticResource MutedTextBrush}" />
                    <ListBox x:Name="DetailLinesList"
                             Grid.Row="4"
                             Margin="0,18,0,18"
                             BorderThickness="0"
                             Foreground="{StaticResource TextBrush}" />
                    <StackPanel Grid.Row="5">
                        <TextBlock x:Name="DetailTotalText"
                                   Margin="0,0,0,14"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}" />
                        <Button Click="ExportCalendarButton_OnClick"
                                Content="Kalendereintrag exportieren"
                                Style="{StaticResource SecondaryButtonStyle}" />
                        <Button Margin="0,8,0,0"
                                Click="ExportEmailButton_OnClick"
                                Content="E-Mail-Entwurf erzeugen"
                                Style="{StaticResource PrimaryButtonStyle}" />
                    </StackPanel>
                </Grid>
            </Border>

            <Border x:Name="EmptyOrderState"
                    Grid.Column="0"
                    Grid.ColumnSpan="3"
                    Padding="32"
                    Background="White"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock HorizontalAlignment="Center"
                               FontSize="24"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}"
                               Text="Keine Verkäufe vorhanden" />
                    <TextBlock MaxWidth="460"
                               Margin="0,10,0,22"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"
                               Foreground="{StaticResource MutedTextBrush}"
                               Text="Lege einen Verkauf an, um einen Partner, Produkte und die Auftragswerte zu erfassen."
                               TextWrapping="Wrap" />
                    <Button HorizontalAlignment="Center"
                            Click="NewOrderButton_OnClick"
                            Content="Neuen Verkauf anlegen"
                            Style="{StaticResource PrimaryButtonStyle}" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
