<UserControl x:Class="SOCDOF.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid Margin="0,28,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel>
                <TextBlock FontSize="14"
                           Foreground="{StaticResource MutedTextBrush}"
                           Text="Sortiment, Preise und Bestände lokal verwalten" />
                <TextBlock x:Name="ProductCountText"
                           Margin="0,8,0,0"
                           FontSize="12"
                           Foreground="{StaticResource MutedTextBrush}" />
            </StackPanel>

            <Button HorizontalAlignment="Right"
                    Click="NewProductButton_OnClick"
                    Content="+  Neues Produkt anlegen"
                    Style="{StaticResource PrimaryButtonStyle}" />
        </Grid>

        <Grid x:Name="ProductToolbar" Grid.Row="1" Margin="0,24,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" MaxWidth="420" HorizontalAlignment="Left">
                <TextBox x:Name="SearchBox"
                         Padding="12,9"
                         FontSize="14"
                         TextChanged="SearchBox_OnTextChanged"
                         ToolTip="Produkte suchen" />
                <TextBlock Margin="14,0,0,0"
                           VerticalAlignment="Center"
                           IsHitTestVisible="False"
                           Foreground="{StaticResource MutedTextBrush}"
                           Text="Produkte suchen...">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <Button Grid.Column="1"
                    Margin="12,0,0,0"
                    Click="EditProductButton_OnClick"
                    Content="Bearbeiten"
                    IsEnabled="False"
                    x:Name="EditProductButton"
                    Style="{StaticResource SecondaryButtonStyle}" />
            <Button Grid.Column="2"
                    Margin="8,0,0,0"
                    Click="DeleteProductButton_OnClick"
                    Content="Löschen"
                    IsEnabled="False"
                    x:Name="DeleteProductButton"
                    Style="{StaticResource SecondaryButtonStyle}" />
        </Grid>

        <DataGrid x:Name="ProductGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  IsReadOnly="True"
                  SelectionChanged="ProductGrid_OnSelectionChanged"
                  SelectionMode="Single"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowHeaderWidth="0"
                  Background="White"
                  BorderBrush="{StaticResource BorderBrush}"
                  BorderThickness="1"
                  Foreground="{StaticResource TextBrush}">
            <DataGrid.RowStyle>
                <Style TargetType="{x:Type DataGridRow}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                            <Setter Property="Background" Value="#FFF4E5" />
                            <Setter Property="Foreground" Value="#8A4B08" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="SKU" Binding="{Binding SKU}" Width="1.2*" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" />
                <DataGridTextColumn Header="Preis" Binding="{Binding Price, StringFormat={}{0:N2}}" Width="1.1*" />
                <DataGridTextColumn Header="Bestand" Binding="{Binding StockQuantity, StringFormat={}{0:N2}}" Width="1.2*" />
                <DataGridTextColumn Header="Einheit" Binding="{Binding Unit}" Width="1.1*" />
            </DataGrid.Columns>
        </DataGrid>

        <Border x:Name="EmptyProductState"
                Grid.Row="2"
                Padding="32"
                Background="White"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock HorizontalAlignment="Center"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextBrush}"
                           Text="Keine Produkte vorhanden" />
                <TextBlock MaxWidth="440"
                           Margin="0,10,0,22"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           Foreground="{StaticResource MutedTextBrush}"
                           Text="Lege dein erstes Produkt an, damit es im Katalog erscheint."
                           TextWrapping="Wrap" />
                <Button HorizontalAlignment="Center"
                        Click="NewProductButton_OnClick"
                        Content="Neues Produkt anlegen"
                        Style="{StaticResource PrimaryButtonStyle}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
